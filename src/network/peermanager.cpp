#include "peermanager.h"


/*{
    //open file
    //read addresses
    //try to connect to them
    //set timer to asks for addresses

    //QTcpSocket socket;
    //socket.connectToHost();
    /*Connection *connection = new Connection(this);
    connection->setSocketDescriptor(socketDescriptor);
    emit newConnection(connection);
}

void PeerManager::makeNewConnections()
{

    for(auto adress : addresses){
        //попробовать подключиться
        //если успешно, отправить сигнал в клиент, чтобы он дальше там разбирался
    }

}

void PeerManager::readAddressesFromFile()
{
    std::ifstream file;
    file.open("addresses.dat");
    if(!file.fail()){
        //читаем адреса и добавляем в массив, если они есть
    }
}

void PeerManager::writeAddressesIntoFile()
{
    // получаем список адресов из соединений из клиента
    // сохраняем в файл
    //    std::ofstream o("pretty.json");

}

void PeerManager::getAddresses()
{

}

void PeerManager::askAddresses()
{
    //просим клиент отослать
}
*/
